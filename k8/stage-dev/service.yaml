apiVersion: v1
kind: Service
metadata:
  name: did-solution-backend
  namespace: common-system
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: app-did-solution-backend
