배포된 리소스 삭제
kubectl delete -f .

리소스 배포
kubectl apply -f .

배포 후 kubectl get pod 명령어 수행 시 did-service-backend 이름이 붙은 자원의 STATUS가 RUNNING 상태로 변경된 것을 확인하시면 정상 배포 상태입니다.

로그 확인
kubectl get pod
에서 did-service-backend 이름이 붙은 자원 이름 복사
kubectl logs '복사한 이름' -c did-service-backend
